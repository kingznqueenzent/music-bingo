# Optimized wedding photos (web)

Web-ready files only (JPG/WebP). Keep camera originals in `/assets/raw/weddings/`.

**Currently wired paths** live under `/assets/images/events/weddings/` (`wedding-ceremony.jpg`, `wedding-dancefloor.jpg`). Those files are atmosphere stand-ins — replace with real Kingz wedding photography (same filenames or update `config/site-config.js`).

Drop additional optimized wedding photos here when you have them. Do not put RAW files here. Do not add AI/stock people as the DJs.
