/** Seconds between auto-played tracks after each clip ends (host dashboard). */
export const DEFAULT_GAME_PACE_SECONDS = 35

/** Host UI presets for auto-play pace (seconds). */
export const GAME_PACE_PRESETS = [15, 25, 35, 45] as const
